# 统一换行符: 前端文件强制 LF
# 否则 Windows 检出 CRLF 会让 biome format 报出一堆假错误
# (CI 在 Linux 上跑,本地与 CI 结果必须一致)
*.ts text eol=lf
*.tsx text eol=lf
*.vue text eol=lf
*.js text eol=lf
*.json text eol=lf
*.css text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.py text eol=lf
# shell 脚本必须 LF——CRLF 检出会让 bash 报 $'\r': command not found
*.sh text eol=lf
# 批处理必须 CRLF 入库:GitHub ZIP 直发 blob 字节(无检出转换),
# LF 批处理在部分 Windows 构建上 goto 标签匹配失败/cmd 按字节劈行。
# text eol=crlf 会把 blob 归一成 LF——故改用 -text 原样入库(工作区保持 CRLF)
*.bat -text
